{
  "name": "lace.js",
  "version": "0.2.0",
  "description": "Tie up your calls like you tie up your shoes",
  "scripts": {
    "build": "npm run --silent build-es ; npm run --silent build-cjs ; npm run --silent build-umd",
    "build-es": "rm -f -r dist/es && mkdir -p dist/es && tsc -p tsconfig.json",
    "build-cjs": "rm -f -r dist/cjs && mkdir -p dist/cjs && rollup -c -f cjs -o dist/cjs/index.js",
    "build-umd": "rm -f -r dist/umd && mkdir -p dist/umd && rollup -c -f umd -n lace -o dist/umd/index.js",
    "test": "cd test ; ts-node index.tape"
  },
  "keywords": [
    "chaining",
    "tooling"
  ],
  "author": "André M. Psiorz <andre.m.arnold@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "rollup": "^0.56.5",
    "tape": "^4.9.0",
    "ts-node": "^5.0.1",
    "typescript": "^2.7.2"
  }
}
